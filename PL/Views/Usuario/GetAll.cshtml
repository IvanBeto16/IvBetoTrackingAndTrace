@model ML.Usuario
@{
    ViewBag.Title = "GetAll";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="https://use.fontawesome.com/releases/v6.1.0/js/all.js" crossorigin="anonymous"></script>

<h2 class="text-center">Usuarios Registrados</h2>
<br />
<br />
<span>Lista de los usuarios registrados en el sistema.</span>
<hr />
@if (Model.Usuarios.Count > 0)
{
    <table class="table table-responsive bg-dark table-bordered table-striped">
        <thead>
            <tr>
                <th>Editar Usuario</th>
                <th>Nombre</th>
                <th>Nombre de Usuario</th>
                <th>Correo Electronico</th>
                <th>Tipo de Rol</th>
                <th>Eliminar Usuario</th>
            </tr>
        </thead>
        <tbody>
            @foreach (ML.Usuario usu in Model.Usuarios)
            {
                <tr>
                    <td><a class="btn btn-info" href="@Url.Action("Form", "Usuario", new { idUsuario = usu.IdUsuario })"><i class="fa-solid fa-pencil" style="color: #0c45a7;"></i> Editar</a></td>
                    <td>@usu.Nombre @usu.ApellidoPaterno @usu.ApellidoMaterno</td>
                    <td>@usu.Username</td>
                    <td>@usu.Email</td>
                    <td>@usu.Rol.Tipo</td>
                    <td><a class="btn btn-danger"><i class="fa-solid fa-user-pen"></i> Eliminar</a></td>
                </tr>
            }

        </tbody>
    </table>
}
else
{
    <div class="alert alert-success" role="alert">
        <h4 class="alert-heading">Mira cuanto vacío!</h4>
        <p>Parece que en este momento no tienes usuarios de ningun tipo registrados en el sistema, por lo que es curioso como pudiste ingresar aqui.</p>
        <hr>
        <p class="mb-0">Se pueden agregar nuevos usuarios desde el login o si tienes el rol del administrador.</p>
    </div>
}
<span>Para agregar nuevos usuarios (Admin) al sistema, haga click en el siguiente botón para llenar el formulario de registro: </span>
<br />
<center>
    <a class="btn btn-success" href="@Url.Action("Form", "Usuario", new { idUsuario = 0 })"> Agregar Usuario</a>
</center>

